# Lucifer

He brings the light.

## Setup

```bash
cd client && yarn
```

## Do the thing

In mandelbrot:

```bash
cargo watch -i .gitignore -i "pkg/*" -s "wasm-pack build
```

In client:

```bash
yarn start
```
